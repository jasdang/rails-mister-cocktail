<div class="cocktail-banner" style="background-image: linear-gradient(rgba(0, 0, 0,0.5), rgba(0,0,0,0.5)), url(<%= cl_image_path @cocktail.photo.key %>); ">
  <h1><%= @cocktail.name %></h1>
  <p><%= link_to 'Delete this cocktail', cocktail_path(@cocktail), method: :delete, data: { confirm: "Are you sure?"} %></p>
</div>


<div class="container mt-5">
  <div class="row justify-content-center text-center">
    <div class="col-md-6">
      <h3>Ingredients</h3>
    </div>
  </div>
<!--   <div class="row">
    <div class="col-md-6">
 -->      <% @cocktail.doses.each do |dose| %>
        <div class="row justify-content-center">
          <div class="col-md-3">
            <%= "#{dose.ingredient.name} - #{dose.description}" %>
          </div>
          <div class="col-md-1">
            <%= link_to 'Delete', cocktail_dose_path(@cocktail, dose), method: :delete, data: { confirm: "Are you sure?"} %>
          </div>
        </div>
      <% end %>
<!--     </div>
  </div>
 -->  <div class="row justify-content-center">
    <div class="col-md-4">
      <p><%= link_to 'Add an ingrdient', new_cocktail_dose_path(@cocktail), class: 'btn btn-primary mt-3' %></p>
    </div>
  </div>
  <div class="row justify-content-center">
    <div class="col-md-4">
      <p><%= link_to 'Back', cocktails_path %></p>
    </div>
  </div>
</div>
